# Linux - Proof of Compromise (bewijs verzamelen)
# One-liner voor screenshot/rapport:
echo " ";echo "uname -a:";uname -a;echo " ";echo "hostname:";hostname;echo " ";echo "id";id;echo " ";echo "ifconfig:";/sbin/ifconfig -a;echo " ";echo "proof:";cat /root/proof.txt 2>/dev/null; cat /Desktop/proof.txt 2>/dev/null;echo " "
# Uitgebreide variant:
echo "=== PROOF OF COMPROMISE ===" && echo "Date: $(date)" && echo "User: $(whoami)" && echo "ID: $(id)" && echo "Hostname: $(hostname)" && echo "Kernel: $(uname -a)" && echo "IP:" && ip addr show | grep "inet " && echo "proof.txt:" && cat /root/proof.txt 2>/dev/null && cat /home/*/proof.txt 2>/dev/null
# Netwerk info:
ip addr show
ss -tlnp
cat /etc/resolv.conf
# Tip: Maak altijd een screenshot van de output
# Tip: Check ook: /root/flag.txt, /home/*/user.txt (CTF/HTB)
# Tip: 'ip addr' vervangt 'ifconfig' op moderne distro's
# Tip: Noteer datum/tijd voor in het rapport
